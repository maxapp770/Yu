# أمان النسخة المحلية 1.8

admin / 123456 حساب مراجعة ثابت أضيف بطلب المستخدم. لا يمثل مصادقة أو صلاحية إنتاجية؛ يجب إزالته من بناء الإنتاج. لا يوجد اتصال بنظام ERPNext أو مفاتيح API في APK.

- تصريح الكاميرا فقط؛ لا INTERNET أو تصاريح تخزين أو تسجيل صوت. تُطلب الكاميرا عند فتح القارئ، مع إدخال يدوي عند الرفض.
- BarcodeActivity غير مصدّرة، ولا يمكن لتطبيق خارجي استدعاؤها مباشرة. الإطلاق من الأصل المحلي المسموح ومن نقرة صريحة.
- لا جسر JavaScript عام. تعاد نتيجة قراءة محدودة الطول إلى مستهلك منتظر، بترميز JSON، ولا تُفتح عناوين QR تلقائياً.
- إغلاق Camera2 وImageReader والخيط عند مغادرة القارئ. الصور في الذاكرة فقط، لا ملفات أو رفع.
- منع file/content والمحتوى المختلط وشبكة WebView، وتعطيل debugging والنسخ الاحتياطي. تسريع العتاد مفعّل لقارئ الكاميرا وحده لأن TextureView يتطلبه.
- جلسة الإدارة في الذاكرة، مع التحقق قبل الحفظ والتعارض والخروج وإهمال الردود المتأخرة. أثناء تسليم التحكم للقارئ الداخلي يحتفظ النموذج بمكانه؛ تستمر حدود المهلة والتحقق من الصلاحية عند العودة.
- نتائج البحث الخارجي تتحقق من الأكواد والأسماء والأسعار والحدود قبل العرض. لا يُقرأ الدور من نتيجة صنف أو التخزين أو الباركود.
- السلة ومسودات الطلبات محلية، ولا تُعتمد أسعارها أو packed_items كسلطة خادمية. على ERPNext إعادة البناء والتسعير والصلاحيات ومنع التكرار.

لم يُنفذ اختبار اختراق مستقل أو تجربة كاميرا هاتف فعلي. بيانات localStorage ليست مشفرة إضافة إلى عزل التطبيق ولا تصلح لأسرار الإنتاج. التوقيع المرفق توقيع اختبار مستمر، لا توقيع المتجر النهائي.

المراجع: [طلب إذن وقت التشغيل في Android](https://developer.android.com/training/permissions/requesting)، [إصدارات ZXing](https://github.com/zxing/zxing/releases)، [صلاحيات Frappe](https://docs.frappe.io/framework/user/en/basics/users-and-permissions).

## مدخل الإدارة داخل تطبيق العميل

يمكن أن يوجد مدخل الإدارة في تطبيق العميل؛ يجب أن يتحقق الخادم من الهوية والدور وصلاحيات كل عملية عند الربط. إخفاء الزر لا يكفي. حساب admin / 123456 الحالي طلبه صاحب التطبيق للمعاينة المحلية، وليس حماية مناسبة لنسخة العملاء الإنتاجية. يجب تعطيل هذا الناقل التجريبي وإزالة الحساب الثابت قبل ربط الخدمات الحقيقية. لا يحتوي APK الحالي على مفاتيح ERPNext، ولا يتصل بخدماته. يمكن استخدام لوحة منفصلة مستقبلاً، لكنها لا تغني عن التحقق على الخادم.


دورة التوصيل الجديدة تتحقق من الدور والمرحلة ونسخة الطلب، وتكتب الحالة والحدث والتحصيل معاً على الجهاز. الإشعار يفتح وجهة معتمدة دون تنفيذ HTML أو عناوين برمجية. حساب المراجعة المحلي ليس حاجز تفويض إنتاجياً؛ يجب إعادة التحقق من ملكية الطلب والإسناد ودور System Manager على الخادم عند الربط. لا توجد خدمة Push أو بيانات اعتماد خادم في هذه الإضافة.
