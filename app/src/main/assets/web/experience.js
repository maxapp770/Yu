'use strict';
/* Phase 1.3: video layouts and local customer journeys. No remote authority. */
window.Experience=(()=>{
 let ui,entered=false,slide=0,paused=false,timer,tick,flashQuery='',priceMin=null,priceMax=null,brands=[],sections=[],orderFilter='all',orderQuery='',messageQuery='',notificationTab='all';
 let mode='ready',pendingReturn='home',loadVersion=0;const begun=Date.now();
 const dictionary={"الرئيسية": "Home", "تسوق": "Shop", "العروض": "Offers", "طلباتي": "My orders", "حسابي": "My account", "المفضلة": "Favorites", "سلة التسوق": "Shopping cart", "الإشعارات": "Notifications", "بحث": "Search", "البحث": "Search", "ابحث هنا": "Search here", "ابحث عن منتجاتك المفضلة": "Search your favorite products", "ابحث عن منتجاتك المفضلة...": "Search your favorite products...", "البحث بكود المنتج": "Search by product code", "رجوع": "Back", "مشاركة": "Share", "عالمية": "Global", "تصفية": "Filter", "ترتيب": "Sort", "إعادة ضبط": "Reset", "الكل": "All", "✓ الكل": "✓ All", "وصل حديثاً": "New arrivals", "منتجات مميزة": "Featured products", "كل المنتجات": "All products", "عرض المزيد": "View more", "جديد": "New", "عروض خاصة": "Special offers", "لا توجد تقييمات بعد": "No ratings yet", "لا توجد تقييمات": "No ratings", "خيارات المنتج": "Product options", "اختيار اللون والحجم": "Choose color and size", "المنتج": "Product", "المنتج غير موجود": "Product not found", "عد إلى قائمة المنتجات.": "Return to the product catalog.", "كود التسويق:": "Product code:", "السعر:": "Price:", "السعر": "Price", "العلامة التجارية:": "Brand:", "العلامة التجارية": "Brand", "الكمية": "Quantity", "الوحدة": "Unit", "المجموع الإجمالي:": "Total:", "أضف إلى السلة": "Add to cart", "وصف المنتج": "Product description", "تفاصيل المنتج": "Product details", "العودة إلى أعلى": "Back to top", "العودة إلى الرئيسية": "Back to home", "متابعة التسوق": "Continue shopping", "متابعة الطلب": "Continue order", "سلتك فارغة": "Your cart is empty", "أضف منتجاً أولاً.": "Add a product first.", "تصفح المنتجات": "Browse products", "لا توجد نتائج": "No results", "جرّب كلمة أخرى أو أزل خيارات التصفية.": "Try another search or clear filters.", "حذف المنتج": "Remove product", "حذف": "Delete", "إلغاء": "Cancel", "تراجع": "Undo", "نسخ": "Copy", "تم النسخ": "Copied", "إغلاق": "Close", "حسناً": "OK", "اسم المستلم": "Recipient name", "رقم الجوال": "Mobile number", "عنوان التسليم": "Delivery address", "ملاحظات": "Notes", "حفظ مسودة الطلب": "Save order draft", "هذه نسخة تجريبية. يمكنك حفظ مسودة محلية؛ لن يُرسل طلب ولن يتم تحصيل أي مبلغ.": "This is a preview. Save a local draft; no order is sent and no payment is collected.", "الشحن والضرائب غير محتسبة في هذه المرحلة.": "Shipping and taxes are not calculated in this phase.", "إجمالي المنتجات:": "Products subtotal:", "مسودة محلية": "Local draft", "لم يتم إرسالها": "Not sent", "عناويني": "My addresses", "تُحفظ العناوين على هذا الجهاز فقط.": "Addresses are saved on this device only.", "إضافة عنوان": "Add address", "اسم العنوان": "Address name", "المنزل أو العمل": "Home or work", "العنوان": "Address", "حفظ العنوان": "Save address", "حذف العنوان": "Delete address", "تسجيل الدخول": "Sign in", "كلمة المرور": "Password", "أهلاً بك في الهيثم": "Welcome to HEC", "تسجيل الدخول سيتاح عند تفعيل المتجر. يمكنك الآن المتابعة كضيف.": "Sign-in will be available when the store is enabled. You can continue as a guest.", "المتابعة كضيف": "Continue as guest", "نسيت كلمة المرور؟": "Forgot password?", "المساعدة والتواصل": "Help and contact", "كيف يمكننا مساعدتك؟": "How can we help?", "كيف أضيف منتجاً للسلة؟": "How do I add a product to the cart?", "أين تُحفظ بياناتي؟": "Where is my data saved?", "هل يتم إرسال الطلب؟": "Is the order sent?", "اختر الكمية واضغط على رمز السلة، أو افتح تفاصيل المنتج ثم اضغط «أضف إلى السلة».": "Choose a quantity and tap the cart icon, or open the product and tap Add to cart.", "السلة والمفضلة والعناوين ومسودات الطلبات محفوظة محلياً على هذا الجهاز. حذف بيانات التطبيق يزيلها.": "Cart, favorites, addresses and order drafts are stored locally on this device. Clearing app data removes them.", "لا، هذه مرحلة بناء الواجهات. حفظ المسودة لا يعني إرسال الطلب أو تأكيد الشراء.": "This is the interface phase. Saving a draft does not send an order or confirm a purchase.", "الخصوصية وبيانات الجهاز": "Privacy and device data", "أنت تتحكم في بياناتك": "You control your data", "تعمل هذه النسخة محلياً. لا ترسل طلبات أو بيانات حساب إلى خادم.": "This version works locally. It does not send orders or account data to a server.", "بيانات التسوق": "Shopping data", "مسح بيانات التسوق": "Clear shopping data", "معاينة المندوب": "Courier preview", "مسح بيانات المندوب": "Clear courier data", "الملف المحلي وحالات التوصيل التجريبية.": "Local profile and demo delivery statuses.", "حماية الحساب": "Account protection", "لا نحفظ كلمات مرور. بيانات المعاينة محفوظة داخل مساحة التطبيق على جهازك. قفل الشاشة يحمي الوصول إلى الجهاز.": "Passwords are not saved. Preview data is stored in the app's device storage. Your screen lock protects access to the device.", "تسجيل الدخول والصلاحيات والأسعار المعتمدة ستتاح بعد ربط الخدمة.": "Sign-in, permissions and verified prices will be available after the service is connected.", "العودة إلى حسابي": "Back to my account", "أدوات المطبخ": "Kitchenware", "سيراميك وميلامين": "Ceramic and melamine", "زجاجيات": "Glassware", "أدوات البهارات والزيت": "Spice and oil accessories", "بلاستيكية": "Plasticware", "أدوات مساعدة للمطبخ": "Kitchen accessories", "منتجات على المتجر": "Store products", "عرض الكل": "View all", "أجهزة وإلكترونيات": "Appliances and electronics", "أجهزة كهربائية": "Electrical appliances", "العناية الشخصية": "Personal care", "مراوح ومكيفات": "Fans and air conditioners", "شاشات وأفران": "Screens and ovens", "كنسات وكوايات": "Vacuums and irons", "أجهزة خفيفة": "Small appliances", "مايكروويف وأفران": "Microwaves and ovens", "غسالات وثلاجات": "Washers and refrigerators", "أدوات الكيك": "Baking tools", "قوالب الكيك": "Cake molds", "أدوات التقديم": "Serving tools", "علب الزيت والعسل": "Oil and honey containers", "متعلق بالمعدن": "Metalware", "سلات بلاستيك كبيرة": "Large plastic baskets", "سلات خضار أربع طبقات": "Four-tier vegetable racks", "كراسي مطبخ": "Kitchen stools", "دراجات هوائية وسيارات كهربائية": "Bicycles and electric cars", "اسكوتر": "Scooters", "دراجات أطفال": "Children's bicycles", "سيارات أطفال": "Children's cars", "منظفات": "Cleaning products", "منظفات بلاط وأرضيات": "Tile and floor cleaners", "منظفات الملابس": "Laundry detergents", "منظفات الزجاج": "Glass cleaners", "مفروشات": "Home furnishings", "دعسات مطبخ": "Kitchen mats", "دعسات متنوعة": "Assorted mats", "دعسات أبواب": "Door mats", "ألعاب": "Toys", "مبتكرة": "Innovative", "إدارة الرسائل": "Manage messages", "إيقاف تبديل البنرات": "Pause banner rotation", "تشغيل تبديل البنرات": "Resume banner rotation"};
 const state=()=>ui.state(), E=v=>ui.esc(v), I=k=>ui.icon(k);
 function T(ar,en){dictionary[ar]=en;return ui&&state().locale==='en'?en:ar}
 const note=()=>`<p class="demo-ribbon">${T('معاينة محلية — المسودات لا تُرسل ولا تُنفذ أي مدفوعات.','Local preview — drafts are not sent and no payments are processed.')}</p>`;
 const route=()=>location.hash.slice(1).split('/')[0]||'home';
 const commit=f=>ui.commit(f);
 const save=f=>{if(!commit(f))return false;ui.render();ui.toast(T('حُفظت التغييرات على هذا الجهاز','Changes saved on this device'));return true};
 function id(prefix,rows){let n=1,v;do{v=prefix+'-'+Date.now().toString(36).toUpperCase()+'-'+n++}while(rows.some(x=>x.id===v));return v}
 const date=()=>new Date().toISOString();
 const dateLabel=value=>{const d=new Date(value);return Number.isNaN(d.getTime())?E(value):d.toLocaleDateString(state().locale==='en'?'en-GB':'ar-YE')};
 const btn=(label,action,extra='')=>`<button class="primary" data-action="${action}" ${extra}>${label}</button>`;
 const field=(label,name,value='',type='text',max=100,required=true)=>`<label class="form-field">${label}<input name="${name}" type="${type}" value="${E(value)}" maxlength="${max}" ${required?'required':''} ${type==='password'?'autocomplete="new-password"':type==='email'?'autocomplete="email"':type==='tel'?'inputmode="tel" autocomplete="tel"':''}></label>`;
 const area=(label,name,value='',max=500)=>`<label class="form-field">${label}<textarea name="${name}" maxlength="${max}">${E(value)}</textarea></label>`;
 const empty=(title,body)=>`<div class="empty compact-empty">${I('orders')}<h2>${title}</h2><p>${body}</p></div>`;
 function page(title,body,extra='',nav=false){return `${ui.title(title,extra)}<main class="experience-page">${body}</main>${nav?ui.nav():''}`}
 function protectedPage(name,body){if(entered)return body();pendingReturn=name;return page(T('معاينة الحساب','Account preview'),`<div class="preview-gate"><img src="assets/logo.jpg" alt="HEC"><h2>${T('أهلاً بك في حسابك','Welcome to your account')}</h2><p>${T('يمكنك تجربة إدارة الحساب ببيانات محلية. الدخول الحقيقي سيتاح بعد ربط المتجر.','Try account management with local data. Live sign-in will be available when the store is connected.')}</p>${btn(T('فتح معاينة الحساب','Open account preview'),'customer-enter')}<button class="secondary" data-go="login">${T('تسجيل الدخول','Sign in')}</button></div>`)}
 function countdown(c,now=Date.now()){
  if(c.startsAt&&now<Date.parse(c.startsAt))return '<strong>يبدأ العرض '+E(new Date(c.startsAt).toLocaleString('ar'))+'</strong>';
  const end=c.endsAt?Date.parse(c.endsAt):(state().campaignEpoch||now)+c.duration,seconds=Math.max(0,Math.floor((end-now)/1000));
  if(!seconds)return `<strong class="expired">${T('انتهى العرض','Offer ended')}</strong>`;
  return `<span class="countdown" role="timer" aria-label="${T('الوقت المتبقي للعرض التجريبي','Demo offer time remaining')}">${[[Math.floor(seconds/86400),T('ي','d')],[Math.floor(seconds/3600)%24,T('س','h')],[Math.floor(seconds/60)%60,T('د','m')],[seconds%60,T('ث','s')]].map(([n,u])=>`<b>${String(n).padStart(2,'0')} <small>${u}</small></b>`).join('')}</span>`
 }
 function expired(c){return c.active===false||!!c.startsAt&&Date.now()<Date.parse(c.startsAt)|| (c.endsAt?Date.now()>=Date.parse(c.endsAt):!!state().campaignEpoch&&Date.now()>=state().campaignEpoch+c.duration)}
 function flashCard(c,featured=false){return `<article class="flash-card ${featured?'featured-campaign':''}"><button class="flash-image" data-go="flash/${c.id}" aria-label="${E(c.title)}"><img src="${window.HECMedia?HECMedia.url(c.image):'assets/'+c.image}" alt="${E(c.title)}" loading="lazy"></button><div class="golden-label">ϟ ${E(c.title)}</div><div class="flash-overlay"><span>${T('يتبقى','Ends in')}</span><span data-countdown="${c.id}">${countdown(c)}</span><span class="lightning">ϟ</span><button data-go="flash/${c.id}">${T('عرض','View')} ‹</button></div>${Commerce.summary(c)}${featured?`<div class="campaign-products">${c.ids.slice(0,6).map(id=>{const p=HEC.products.find(p=>p.id===id);return `<button data-go="detail/${p.id}" aria-label="${E(p.name)}"><img loading="lazy" src="${E(HECMedia.url(p.image))}" alt="${E(p.name)}"><small>${ui.money(p.price)}</small></button>`}).join('')}</div>`:''}</article>`}
 function promotion(image,label,category){return `<button class="banner-button interleaved-promo" data-promo="${E(label)}" data-subcategory="${E(category||label)}"><img class="banner" src="${E(HECMedia.url(image))}" alt="${E(label)}" loading="lazy"></button>`}
 function tileRail(s){return `<section class="reference-section"><div class="section-heading"><h2>${E(s.displayName||s.name)}</h2>${s.showMore===false?'':`<button data-subcategory="${E(s.name)}">${T('عرض الكل','View all')}</button>`}</div><div class="category-rail" aria-label="${E(s.name)}">${s.children.map(([name,img,target,copy={}])=>{const shown=copy.specialMode==='hide'?false:copy.specialMode==='show'?true:s.showSpecial!==false,text=copy.specialText||(s.specialText??T('عرض خاص','Special offer'));return `<button ${name==='مبتكرة'?'data-go="innovative"':`data-subcategory="${E(target||name)}"`}><img loading="lazy" src="${E(HECMedia.url(img))}" alt=""><span class="category-copy"><span class="category-name">${E(name)}</span>${shown&&text?`<b class="category-promo">${E(text)}</b>`:''}</span></button>`}).join('')}</div></section>`}
 function footer(){return `<footer class="store-footer"><img src="${E(HECMedia.url(HEC.content.footerImage||'logo.jpg'))}" alt="HEC"><p>${E(T(Commerce.content('home.footer','كل احتياجاتك المنزلية في مكان واحد'),'Your home essentials in one place'))}</p><div class="social-links">${[['youtube','YouTube','▶'],['facebook','Facebook','f'],['instagram','Instagram','◎'],['whatsapp','WhatsApp','◉']].map(([key,label,glyph])=>`<button class="social ${key}" data-action="external" data-key="${key}" aria-label="${label}">${glyph}</button>`).join('')}</div><button class="secondary" data-action="external" data-key="play">Google Play</button><button class="text-link" data-go="privacy">${T('الخصوصية وبيانات الجهاز','Privacy and device data')}</button></footer>`}
 function campaignCarousel(){if(slide>=HEC.homeCampaigns.length)slide=0;const c=HEC.homeCampaigns[slide];return `<section class="campaign-carousel" data-experience-swipe="campaign"><button id="campaign-link" class="banner-button" data-go="${c[2]}"><img id="campaign-image" src="${E(HECMedia.url(c[0]))}" alt="${E(c[1])}"></button><div class="carousel-controls"><div class="dots">${HEC.homeCampaigns.map((c,i)=>`<button data-action="campaign-slide" data-index="${i}" aria-label="${E(c[1])}" aria-pressed="${i===slide}" class="${i===slide?'selected':''}"></button>`).join('')}</div><button class="carousel-pause" data-action="campaign-pause" aria-label="${T('إيقاف أو تشغيل تبديل العروض','Pause or play promotions')}">${paused?'▷':'Ⅱ'}</button></div></section>`}
 function home(){const marketing=HECMarketing.home({ui,flashCard,footer,tileRail});if(marketing!==null)return marketing;const shortcuts=HEC.content.shortcuts,configuredGroups=HEC.content.homeGroups;const shownGroups=configuredGroups?configuredGroups.map(r=>{const original=HEC.homeSections.find(x=>x.name===r.originalName)||HEC.homeSections[Number(String(r.sectionId).split('-').pop())]||HEC.homeSections[0];const groups=(r.groupIds||[]).map(id=>HEC.shopGroups.find(g=>g.id===id)).filter(Boolean);return {...original,displayName:r.title,banner:r.image,children:groups.length?groups.map(g=>{const d=HEC.departments.find(d=>d.name===g.name);return [g.name,d?d.image:'logo.jpg']}):(Array.isArray(r.children)?r.children.map(c=>[c.title,c.image,c.target,{specialText:c.specialText,specialMode:c.specialMode}]):original.children),showMore:r.showMore,specialText:r.specialText,showSpecial:r.showSpecial,productIds:r.productIds||[]}}):HEC.homeSections;
 const blocks={
 shortcuts:()=>`<section class="home-shortcuts" aria-label="${T('الأقسام السريعة','Quick categories')}">${shortcuts.map(([n,img,target])=>`<button data-subcategory="${E(target||n)}"><span><img src="${E(HECMedia.url(img))}" alt=""></span><b>${E(n)}</b></button>`).join('')}<button data-go="shop"><span class="all-shapes">${I('grid')}</span><b>${T('عرض الكل','View all')}</b></button></section>`,
 banners:()=>campaignCarousel(),
 deals:()=>`<section class="deal-section"><h2>${E(T(Commerce.content('home.deals','تسوق بأقل الأسعار | عروض وصفقات'),'Shop for less | Offers and deals'))} ${I('cart')}</h2><div class="deals-rail">${HEC.content.deals.map(([img,cat])=>`<button data-subcategory="${E(cat)}"><img src="${E(HECMedia.url(img))}" alt="${E(cat)}" loading="lazy"></button>`).join('')}</div></section>`,
 golden:()=>`<section class="flash-rail">${(HEC.content.goldenIds||[HEC.campaigns[0]?.id]).map(id=>HEC.campaigns.find(c=>c.id===id&&c.active!==false)).filter(Boolean).map(c=>flashCard(c)).join('')}</section>`,
 groups:()=>shownGroups.map(g=>promotion(g.banner,g.name)+tileRail(g)+(g.productIds&&g.productIds.length?`<div class="product-rail">${g.productIds.map(id=>HEC.products.find(p=>p.id===id)).filter(Boolean).map(ui.card).join('')}</div>`:'')).join(''),
 suggestions:()=>`<section class="section home-banner"><div class="section-heading"><h2>${E(T(Commerce.content('home.suggestions','مقترحات الهيثم'),'HEC recommendations'))}</h2></div>${HEC.content.suggestions?HEC.content.suggestions.map(r=>`<button class="banner-button" data-go="shop"><img class="banner" src="${E(HECMedia.url(r.image))}" alt="${E(r.title)}"></button>${r.productIds&&r.productIds.length?ui.grid(r.productIds.map(id=>HEC.products.find(p=>p.id===id)).filter(Boolean)):''}`).join(''):ui.carousel('home')}</section>`,
 new:()=>`<section class="section"><div class="section-heading"><h2>${E(T(Commerce.content('home.new','وصل حديثاً'),'New arrivals'))}</h2><button data-go="catalog/new">${T('عرض الكل','View all')}</button></div><div class="product-rail" aria-label="${T('وصل حديثاً','New arrivals')}">${HEC.content.newIds.map(id=>HEC.products.find(p=>p.id===id&&!p.hidden)).filter(Boolean).map(ui.card).join('')}</div></section>`,
 best:()=>`<section class="section"><div class="section-heading"><h2>${E(T(Commerce.content('home.best','الأكثر مبيعاً'),'Best sellers'))}</h2></div>${ui.grid(HEC.products.filter(p=>HEC.content.bestIds.includes(p.id)&&!p.hidden))}</section>`,footer:()=>footer()};
 const order=Array.isArray(HEC.content.layout)?HEC.content.layout:Object.keys(blocks);return `${ui.header()}<main class="reference-home">${order.filter(k=>blocks[k]).map(k=>blocks[k]()).join('')}<button class="backtop" data-action="top" aria-label="${T('العودة إلى أعلى','Back to top')}">${I('up')}</button></main>${ui.nav()}`}

 function offers(){return page(T((HEC.content.offersLayout||{}).title||'العروض المميزة','Featured offers'),note()+HEC.campaigns.filter(c=>c.active!==false).map(c=>flashCard(c,true)).join(''),ui.action('search',T('بحث في العروض','Search offers'),'data-go="flash/golden"'),true)}
 function flash(){const cid=location.hash.split('/')[1],c=HEC.campaigns.find(c=>c.id===cid);if(!c)return page(T('العروض','Offers'),empty(T('العرض غير موجود','Offer not found'),T('عد إلى صفحة العروض لاختيار عرض متاح.','Choose an available offer from the offers page.')));const ps=HEC.products.filter(p=>c.ids.includes(p.id)&&(!flashQuery||HECStore.search(p.name+' '+p.id).includes(HECStore.search(flashQuery)))&&matches(p));
 return `${ui.title(c.title,ui.action('filter',T('تصفية','Filter'),'data-action="filter"'))}<form id="flash-search-form" class="wine-search" role="search"><input name="q" value="${E(flashQuery)}" placeholder="${T('البحث عن المنتجات من: ','Search products in: ')+E(c.title)}" aria-label="${T('بحث داخل العرض','Search within this offer')}" maxlength="100"><button aria-label="${T('بحث','Search')}">${I('search')}</button><button type="button" data-action="flash-clear" aria-label="${T('مسح البحث','Clear search')}">×</button></form><main class="experience-page">${note()}${flashCard(c)}${expired(c)?empty(T('انتهت مدة هذا العرض','This offer has ended'),T('يمكنك تصفح المنتجات بأسعارها المعروضة دون خصم إضافي.','Products remain available at their displayed prices; no extra discount applies.')):''}<div class="section-band">${T('منتجات العرض','Offer products')} (${ps.length})</div>${ps.length?ui.grid(ps):empty(T('لا توجد نتائج','No results'),T('غيّر البحث أو امسح التصفية.','Change your search or clear filters.'))}</main><button class="floating-cart" data-go="cart" aria-label="${T('السلة','Cart')}">${I('cart')}</button>`}
 function innovative(){return page(T('منتجات على المتجر','Products on the store'),promotion('campaign-global.jpg','منتجات على المتجر')+tileRail(HEC.homeSections[1])+`<h2>${T('مبتكرة','Innovative')}</h2>${ui.grid(HEC.products.filter(p=>['N250','KJ3052','N8051'].includes(p.id)))}`)}
 const defaultMenus=()=>[
 ['profile','إدارة الحساب','Manage account','تعديل الملف الشخصي، تغيير الإيميل، تغيير كلمة المرور','Profile, email and password','user'],
 ['wallet','إدارة المحفظة','Manage wallet','إدارة المحفظة','Wallet overview','store'],
 ['orders','سجل الطلبات','Order history','عرض الطلبات','View your orders','cart'],
 ['returns','طلبات المرتجعات','Return requests','طلبات المرتجعات','Manage return drafts','refresh'],
 ['loyalty','نقاط الولاء','Loyalty points','نقاط الولاء','Points overview','tag'],
 ['affiliate','التسويق بالعمولة','Affiliate marketing','التسويق بالعمولة','Affiliate overview','share'],
 ['messages','إدارة الرسائل','Manage messages','إدارة الرسائل','Your message drafts','chat'],
 ['favorites','قائمة المفضلة','Favorites','قائمة المفضلة','Saved products','heart'],
 ['addresses','إدارة العناوين','Manage addresses','إدارة العناوين','Delivery addresses','pin'],
 ['language','اللغة','Language','تغيير اللغة','Change language','grid'],
 ['help','مركز المساعدة','Help center','مركز المساعدة','Help and support','help'],
 ['', 'تسجيل الخروج','Sign out','تسجيل الخروج','End account preview','logout']];
 const menus=()=>{const defaults=defaultMenus();if(!HEC.content.accountMenu)return defaults;return HEC.content.accountMenu.map(r=>{const old=defaults.find(x=>x[0]===r.service);return old?[r.service,r.title,old[2],r.text||old[3],old[4],old[5]]:null}).filter(Boolean).concat(defaults.filter(x=>!x[0]))};
 function account(){const s=state();return `<main class="customer-account"><section class="customer-identity"><img src="assets/logo.jpg" alt="HEC"><h1>${E(entered?s.profile.name||T('عميل تجريبي','Demo customer'):T('أهلاً بك في الهيثم','Welcome to HEC'))}</h1><p>${entered?T('عميل — معاينة محلية','Customer — local preview'):T('تتصفح كضيف','Browsing as a guest')}</p>${!entered?'<button class="text-link" data-action="customer-enter">'+T('معاينة الحساب','Preview account')+'</button>':''}<div class="customer-stats">${[['cart',ui.count(),T('في السلة','In cart')],['favorites',s.favorites.length,T('في المفضلة','Favorites')],['orders',s.orders.length,T('طلباتي','My orders')]].map(([r,n,t])=>`<button data-go="${r}"><strong>${n}</strong><span>${t}</span></button>`).join('')}</div></section><div class="customer-menu">${menus().filter(x=>HECEngagement.visible(x[0])).map(([r,ar,en,sub,suben,icon])=>`<button ${r?`data-go="${r}"`:'data-action="customer-logout"'}><span class="menu-square">${I(icon)}</span><span><b>${T(ar,en)}</b><small>${T(sub,suben)}</small></span><span class="menu-chevron">${I("arrow")}</span></button>`).join('')}</div><div class="account-tools"><button data-go="privacy">${T('الخصوصية','Privacy')}</button><button data-go="courier-login">${T('الاتصال بحساب الموصّل','Courier account')}</button>${window.HECBuild?.role==='customer'?'<button data-go="erp-account">الاتصال بحساب المتجر</button>':Management.entry()}</div></main>${ui.nav()}`}
 function profile(){const p=state().profile;return protectedPage('profile',()=>page(T('إدارة الحساب','Manage account'),note()+`<form id="customer-profile-form"><h2>${T('الملف الشخصي','Profile')}</h2>${field(T('الاسم','Name'),'name',p.name,'text',80)}${field(T('رقم الجوال','Mobile number'),'phone',p.phone,'tel',20)}<button class="primary" type="submit">${T('حفظ الملف الشخصي','Save profile')}</button></form><button class="secondary" data-go="email">${T('تغيير الإيميل','Change email')}</button><button class="secondary" data-go="password">${T('تغيير كلمة المرور','Change password')}</button>`))}
 function email(){return protectedPage('email',()=>page(T('تغيير الإيميل','Change email'),note()+`<form id="customer-email-form">${field(T('الإيميل','Email'),'email',state().profile.email,'email',100)}<p class="notice">${T('يُحفظ عنوان الاتصال محلياً. لم يتم التحقق من ملكيته ولا يتيح تسجيل الدخول.','The contact email is saved locally. Ownership is not verified and it does not enable sign-in.')}</p><button class="primary">${T('حفظ الإيميل محلياً','Save email locally')}</button></form>`))}
 function password(){if(window.HECERP?.context()?.authenticated)return HECERP.passwordPage();return protectedPage('password',()=>page(T('تغيير كلمة المرور','Change password'),`<p class="notice">${T('يمكنك معاينة التحقق من الحقول. تغيير كلمة المرور الفعلي يحتاج خدمة حساب موثقة. لا نحفظ كلمات المرور.','Preview field validation. Changing a real password requires an authenticated account service. Passwords are never stored.')}</p><form id="customer-password-form">${field(T('كلمة المرور الحالية','Current password'),'old','','password',128)}${field(T('كلمة المرور الجديدة','New password'),'new','','password',128)}${field(T('تأكيد كلمة المرور','Confirm password'),'confirm','','password',128)}<p>${T('استخدم 12 محرفاً على الأقل لكلمة المرور الجديدة.','Use at least 12 characters for the new password.')}</p><button class="primary">${T('التحقق من الحقول','Validate fields')}</button><p id="customer-feedback" role="status"></p></form>`))}
 function wallet(){return protectedPage('wallet',()=>page(T('محفظتي','My wallet'),note()+`<section class="balance-card"><span>${T('الرصيد المتاح','Available balance')}</span><strong>${ui.money(0)}</strong><small>${T('لا توجد محفظة مالية مرتبطة','No live wallet is connected')}</small></section><div class="dual-actions"><button class="secondary" data-action="wallet-compose" data-type="deposit">${T('مسودة إيداع','Deposit draft')}</button><button class="secondary" data-action="wallet-compose" data-type="withdraw">${T('مسودة سحب','Withdrawal draft')}</button></div><h2>${T('مسودات الطلبات','Request drafts')}</h2>${state().walletRequests.length?state().walletRequests.map(r=>draftCard(r,`<strong>${r.type==='deposit'?T('إيداع','Deposit'):T('سحب','Withdrawal')} · ${ui.money(r.amount)}</strong><p>${E(r.note)}</p>`,'walletRequests')).join(''):empty(T('لا توجد حركات مالية','No transactions'),T('لم يتم تنفيذ أي إيداع أو سحب.','No deposits or withdrawals have been processed.'))}`))}
 function draftCard(r,body,key){return `<article class="local-record">${body}<div class="record-meta"><span>${T('مسودة محلية','Local draft')} · ${dateLabel(r.date)}</span><button data-action="delete-local" data-kind="${key}" data-id="${r.id}" aria-label="${T('حذف المسودة','Delete draft')}">${I('trash')}</button></div></article>`}
 function returns(){return protectedPage('returns',()=>page(T('طلبات المرتجعات','Return requests'),note()+`${btn(T('طلب مرتجع جديد','New return request'),'return-compose')}${state().returns.length?state().returns.map(r=>draftCard(r,`<strong>${E(HEC.products.find(p=>p.id===r.productId).name)}</strong><p>${E(r.orderId)} · ${E(r.lineKey)} · ${T('الكمية','Quantity')}: ${r.qty}</p><p>${E(r.note)}</p>`,'returns')).join(''):empty(T('لا توجد طلبات مرتجعات','No return requests'),T('يمكنك إنشاء مسودة مرتبطة بمسودة طلب محفوظة.','Create a return draft linked to a saved order draft.'))}`))}
 function loyalty(){return protectedPage('loyalty',()=>page(T('نقاط الولاء','Loyalty points'),note()+`<section class="balance-card"><span>${T('رصيد النقاط','Points balance')}</span><strong>0</strong><small>${T('لا توجد نقاط مكتسبة أو مستبدلة','No earned or redeemed points')}</small></section><h2>${T('كيف تعمل النقاط؟','How do points work?')}</h2><ol class="readable-list"><li>${T('تُحتسب النقاط على الطلبات المعتمدة وفق سياسة المتجر.','Points are calculated for confirmed orders under the store policy.')}</li><li>${T('ستظهر الحركات وتواريخ الصلاحية عند تفعيل الخدمة.','Transactions and expiry dates appear when the service is enabled.')}</li><li>${T('المسودات المحلية لا تكسب نقاطاً ولا تُغيّر الرصيد.','Local drafts do not earn points or change the balance.')}</li></ol>${empty(T('سجل النقاط فارغ','Points history is empty'),T('لم تُسجّل حركات بعد.','No activity yet.'))}`))}
 function affiliate(){return protectedPage('affiliate',()=>{const d=state().affiliateDraft||{};return page(T('التسويق بالعمولة','Affiliate marketing'),promotion('campaign-affiliate.jpg','التسويق بالعمولة','كل المنتجات')+note()+`<div class="customer-stats"><div><strong>0</strong><span>${T('الزيارات','Visits')}</span></div><div><strong>0</strong><span>${T('الطلبات','Orders')}</span></div><div><strong>0</strong><span>${T('العمولات','Commissions')}</span></div></div><p class="notice">${T('العمولات وروابط الإحالة تُصدر من المتجر بعد اعتماد الاشتراك.','The store issues referral links and commissions after approving enrollment.')}</p><form id="affiliate-form"><h2>${T('مسودة طلب اشتراك','Enrollment draft')}</h2>${field(T('الاسم','Name'),'name',d.name||state().profile.name,'text',80)}${field(T('قناة التسويق','Marketing channel'),'channel',d.channel,'text',160)}${area(T('ملاحظات','Notes'),'note',d.note)}<button class="primary">${T('حفظ المسودة','Save draft')}</button>${d.status?'<p class="notice">'+T('مسودة محفوظة — لم تُرسل','Saved draft — not sent')+'</p>':''}</form>`)} )}
 function messages(){return protectedPage('messages',()=>{const rows=state().messages.filter(x=>HECStore.search(x.subject+' '+x.body).includes(HECStore.search(messageQuery)));return page(T('إدارة الرسائل','Manage messages'),note()+`<form id="message-search-form" class="inline-search"><input name="q" value="${E(messageQuery)}" maxlength="100" aria-label="${T('بحث في الرسائل','Search messages')}" placeholder="${T('بحث في الرسائل','Search messages')}"><button aria-label="${T('بحث','Search')}">${I('search')}</button></form>${btn(T('رسالة جديدة','New message'),'message-compose')}${rows.length?rows.map(r=>draftCard(r,`<button class="record-link" data-action="message-open" data-id="${r.id}"><strong>${E(r.subject)}</strong><p>${E(r.body.slice(0,100))}</p></button>`,'messages')).join(''):empty(T('لا توجد رسائل','No messages'),T('اكتب رسالة واحفظها كمسودة على جهازك.','Write a message and save it as a draft on your device.'))}`)})}
 function notifications(){if(window.HECNotifications)return HECNotifications.screen();const rows=HEC.notifications.filter(n=>notificationTab==='all'||!state().readNotifications.includes(n.id));return page(T('الإشعارات','Notifications'),`<div class="tabs"><button data-action="notification-tab" data-tab="all" class="${notificationTab==='all'?'active':''}">${T('الكل','All')}</button><button data-action="notification-tab" data-tab="unread" class="${notificationTab==='unread'?'active':''}">${T('غير مقروءة','Unread')}</button></div><button class="text-link" data-action="notifications-read">${T('تحديد الكل كمقروء','Mark all as read')}</button>${rows.length?rows.map(n=>`<article class="notification ${state().readNotifications.includes(n.id)?'read':'unread'}"><button data-action="notification-open" data-id="${n.id}">${I('bell')}<span><strong>${E(n.title)}</strong><p>${E(n.body)}</p></span></button></article>`).join(''):empty(T('لا توجد إشعارات','No notifications'),T('ستظهر تحديثات المتجر هنا.','Store updates will appear here.'))}`,'',true)}
 function language(){return page(T('اللغة','Language'),`<h2>${T('اختر لغة التطبيق','Choose your app language')}</h2><form id="language-form">${[['ar','العربية'],['en','English']].map(([v,t])=>`<label class="option language-option"><input type="radio" name="locale" value="${v}" ${state().locale===v?'checked':''}>${t}<span>${v==='ar'?'العربية':'English'}</span></label>`).join('')}<button class="primary">${T('تطبيق','Apply')}</button></form><p class="notice">${T('تبقى صور الإعلانات وأسماء المنتجات الأصلية بلغتها المصدر.','Original ad images and catalog names remain in their source language.')}</p>`)}
 function orders(){if(window.HECBuild?.role==='customer'&&window.HECERP?.context()?.customer)return HECERP.customerPage()+ui.nav();const rows=state().orders.filter(o=>(orderFilter==='all'||orderFilter==='draft')&&HECStore.search(o.id+' '+o.customer.name).includes(HECStore.search(orderQuery)));return page(T('سجل الطلبات','Order history'),`<div class="tabs">${[['all',T('الكل','All')],['draft',T('مسودات','Drafts')],['sent',T('مرسلة','Sent')]].map(([v,t])=>`<button data-action="order-tab" data-value="${v}" class="${orderFilter===v?'active':''}">${t}</button>`).join('')}</div>${note()}${window.HECBuild?.role==='customer'?'<button class="secondary" data-go="erp-account">تسجيل الدخول وعرض طلبات النظام</button>':''}<button class="demo-link" data-go="tracking/DEMO-2452">${T('معاينة تتبّع طلب تجريبي','Preview demo order tracking')}</button>${rows.length?rows.map(o=>`<button class="local-record order-link" data-go="order/${o.id}"><strong>${E(o.id)}</strong><span>${T('مسودة محلية','Local draft')}</span><p>${E(o.date)} · ${o.lines.length} ${T('منتجات','products')}</p><b>${ui.money(o.total)}</b> ‹</button>`).join(''):empty(T('لا توجد طلبات','No orders'),orderFilter==='sent'?T('لم تُرسل طلبات فعلية؛ الربط غير مفعّل.','No live orders have been sent; connection is not enabled.'):T('ستظهر مسودات طلباتك هنا.','Your order drafts will appear here.'))}`,ui.action('filter',T('تصفية سجل الطلبات','Filter order history'),'data-action="orders-filter"'),true)}
 function matches(p){return (!(route()==='flash'&&ui.filters().onlyOffers)||p.offer)&&(priceMin===null||p.price>=priceMin)&&(priceMax===null||p.price<=priceMax)&&(!brands.length||brands.includes(p.brand))&&(!sections.length||sections.includes(p.category)||sections.includes(p.subcategory))}
 function filterDialog(){const f=ui.filters(),available=HEC.products.filter(p=>!f.query||HECStore.search(p.name+' '+p.id+' '+p.brand).includes(HECStore.search(f.query))),cats=[...new Set(available.map(p=>p.category))],bs=[...new Set(available.map(p=>p.brand))];ui.dialog(T('تصفية المنتجات','Filter products'),`<form id="filter-form" class="drawer-form"><h3>${T('نطاق الأسعار','Price range')}</h3><div class="price-fields">${field(T('الحد الأدنى','Minimum'),'min',priceMin===null?'':priceMin,'number',12,false)}${field(T('الحد الأعلى','Maximum'),'max',priceMax===null?'':priceMax,'number',12,false)}</div><h3>${T('الفئات','Categories')}</h3>${cats.length?cats.map((c,i)=>`<label class="option"><input type="checkbox" name="section${i}" value="${E(c)}" ${sections.includes(c)?'checked':''}>${E(c)}</label>`).join(''):`<p>${T('لا توجد تصنيفات متاحة','No categories available')}</p>`}<h3>${T('العلامات التجارية','Brands')}</h3>${bs.length?bs.map((b,i)=>`<label class="option"><input type="checkbox" name="brand${i}" value="${E(b)}" ${brands.includes(b)?'checked':''}>${E(b)}</label>`).join(''):`<p>${T('لا توجد علامات تجارية متاحة','No brands available')}</p>`}<label class="option"><input type="checkbox" name="offers" ${f.onlyOffers?'checked':''}>${T('العروض الخاصة فقط','Special offers only')}</label><p class="form-error" id="filter-feedback" role="alert"></p><div class="drawer-footer"><button class="apply-filter" type="submit">${T('تطبيق','Apply')}</button><button class="clear-filter" type="button" data-action="filter-clear">${T('محو','Clear')}</button></div></form>`);const d=document.querySelector('#dialog');d.classList.add('filter-drawer')}
 function parsePrice(v){v=HECStore.digits(String(v||''));if(!v)return null;return /^\d{1,9}$/.test(v)?Number(v):NaN}
 function clearFilters(){priceMin=priceMax=null;brands=[];sections=[]}
 function composeMessage(item,productId=''){item=item||{};ui.dialog(T('مسودة رسالة','Message draft'),note()+`<form id="customer-message-form"><input type="hidden" name="draftId" value="${E(item.id)}"><input type="hidden" name="productId" value="${E(productId||item.productId)}">${field(T('الموضوع','Subject'),'subject',item.subject,'text',100)}${area(T('الرسالة','Message'),'body',item.body,1000)}<button class="primary">${T('حفظ المسودة','Save draft')}</button></form>`)}
 function returnCompose(){const os=state().orders;if(!os.length){ui.dialog(T('طلب مرتجع جديد','New return request'),`<p>${T('احفظ مسودة طلب أولاً لاختيار منتجات منها.','Save an order draft first to choose its products.')}</p><button class="primary" data-go="cart">${T('فتح السلة','Open cart')}</button>`);return}ui.dialog(T('مسودة طلب مرتجع','Return request draft'),note()+`<form id="return-form"><label class="form-field">${T('الطلب والمنتج','Order and product')}<select name="line">${os.map(o=>o.lines.map((l,i)=>`<option value="${o.id}/${i}">${E(o.id)} — ${E(l.name)} ${E(l.variant)} (${l.qty})</option>`).join('')).join('')}</select></label>${field(T('الكمية','Quantity'),'qty','1','number',3)}<label class="form-field">${T('سبب الإرجاع','Return reason')}<select name="reason"><option value="damaged">${T('منتج تالف','Damaged item')}</option><option value="wrong">${T('منتج مختلف','Wrong item')}</option><option value="other">${T('سبب آخر','Other reason')}</option></select></label>${area(T('تفاصيل الطلب','Request details'),'note')}<button class="primary">${T('حفظ المسودة','Save draft')}</button></form>`)}
 function feedback(message,selector='#customer-feedback'){const el=document.querySelector(selector);if(el)el.textContent=message;else ui.toast(message)}
 function external(key){const labels={youtube:'YouTube',facebook:'Facebook',instagram:'Instagram',whatsapp:'WhatsApp',play:'Google Play'};if(!Object.prototype.hasOwnProperty.call(labels,key))return;const url=HEC.externalLinks[key];if(!validExternal(url,key)){ui.dialog(labels[key],`<p>${T('رابط المتجر الرسمي لهذا الحساب لم يظهر كاملاً في المرجع. سيُفعّل بعد إضافة الرابط المعتمد.','The official store destination was not fully visible in the reference. It will be enabled once the verified link is supplied.')}</p><button class="primary" data-action="close">${T('حسناً','OK')}</button>`);return}ui.dialog(labels[key],`<p>${T('سيفتح الرابط في التطبيق الخارجي.','The link opens in the external app.')}</p><p class="external-url" dir="ltr">${E(url)}</p><a class="primary" href="${E(url)}" rel="noreferrer">${T('فتح الرابط','Open link')}</a>`)}
 function validExternal(url,key){if(typeof url!=='string'||url.length>500)return false;try{const u=new URL(url),hosts={youtube:['www.youtube.com','youtube.com','youtu.be'],facebook:['www.facebook.com','facebook.com'],instagram:['www.instagram.com','instagram.com'],whatsapp:['wa.me'],play:['play.google.com']};return u.protocol==='https:'&&!u.username&&!u.password&&(!u.port||u.port==='443')&&hosts[key].includes(u.hostname)&&u.pathname.length>1&&!url.includes('%')}catch(_){return false}}
 function click(b){const a=b.dataset.action;if(!a)return false;
  if(a==='filter'){filterDialog();return true}
  if(a==='filter-clear'){clearFilters();ui.setFilters({category:'الكل',onlyOffers:false});ui.render();return true}
  if(a==='reset'||a==='clear-search'){clearFilters();return false}
  if(a==='campaign-slide'){setSlide(Number(b.dataset.index)||0);return true}
  if(a==='campaign-pause'){paused=!paused;ui.render();return true}
  if(a==='customer-enter'){entered=true;ui.go(pendingReturn==='home'?'account':pendingReturn);return true}
  if(a==='customer-logout'){ui.dialog(T('تسجيل الخروج','Sign out'),`<p>${T('هل تريد إنهاء معاينة الحساب؟ ستبقى مسوداتك محفوظة على الجهاز.','End the account preview? Your drafts remain saved on this device.')}</p>${btn(T('تسجيل الخروج','Sign out'),'customer-confirm-logout')}<button class="secondary" data-action="close">${T('إلغاء','Cancel')}</button>`);return true}
  if(a==='customer-confirm-logout'){entered=false;pendingReturn='home';ui.go('account');return true}
  if(a==='confirm-clear-customer'){entered=false;pendingReturn='home';clearFilters();return false}
  if(a==='wallet-compose'){if(!entered){ui.go('wallet');return true}const type=b.dataset.type==='withdraw'?'withdraw':'deposit';ui.dialog(T('مسودة طلب محفظة','Wallet request draft'),note()+`<form id="wallet-form"><input type="hidden" name="type" value="${type}">${field(T('المبلغ بالريال اليمني','Amount in YER'),'amount','','number',9)}${area(T('ملاحظات','Notes'),'note','',300)}<button class="primary">${T('حفظ المسودة','Save draft')}</button></form>`);return true}
  if(a==='return-compose'){if(entered)returnCompose();else ui.go('returns');return true}
  if(a==='message-compose'){if(entered)composeMessage();else ui.go('messages');return true}
  if(a==='message-open'){const r=state().messages.find(r=>r.id===b.dataset.id);if(r&&entered)composeMessage(r);return true}
  if(a==='product-question'){if(!entered){pendingReturn='messages';ui.go('messages')}else composeMessage({subject:T('استفسار عن المنتج ','Product question ')+b.dataset.id},b.dataset.id);return true}
  if(a==='delete-local'){if(!entered)return true;ui.dialog(T('حذف المسودة','Delete draft'),`<p>${T('هل تريد حذف هذه المسودة؟','Delete this draft?')}</p>${btn(T('حذف','Delete'),'confirm-delete-local',`data-kind="${E(b.dataset.kind)}" data-id="${E(b.dataset.id)}"`)}<button class="secondary" data-action="close">${T('إلغاء','Cancel')}</button>`);return true}
  if(a==='confirm-delete-local'){if(entered&&['messages','returns','walletRequests'].includes(b.dataset.kind))save(s=>s[b.dataset.kind]=s[b.dataset.kind].filter(r=>r.id!==b.dataset.id));return true}
  if(a==='notifications-read'){save(s=>s.readNotifications=HEC.notifications.map(n=>n.id));return true}
  if(a==='notification-open'){const n=HEC.notifications.find(n=>n.id===b.dataset.id);if(n&&commit(s=>s.readNotifications=[...s.readNotifications,n.id])){ui.render();ui.dialog(n.title,`<p>${E(n.body)}</p><button class="primary" data-go="${n.route}">${T('عرض','View')}</button>`)}return true}
  if(a==='notification-tab'){notificationTab=b.dataset.tab==='unread'?'unread':'all';ui.render();return true}
  if(a==='orders-filter'){ui.dialog(T('تصفية سجل الطلبات','Filter order history'),`<form id="orders-filter-form">${field(T('رقم الطلب أو اسم المستلم','Order ID or recipient'),'q',orderQuery,'text',100,false)}<label class="form-field">${T('الحالة','Status')}<select name="status"><option value="all">${T('الكل','All')}</option><option value="draft" ${orderFilter==='draft'?'selected':''}>${T('مسودات','Drafts')}</option><option value="sent" ${orderFilter==='sent'?'selected':''}>${T('مرسلة','Sent')}</option></select></label><button class="primary">${T('تطبيق','Apply')}</button><button type="button" class="secondary" data-action="orders-clear">${T('محو','Clear')}</button></form>`);return true}
  if(a==='order-tab'){orderFilter=['all','draft','sent'].includes(b.dataset.value)?b.dataset.value:'all';ui.render();return true}
  if(a==='orders-clear'){orderFilter='all';orderQuery='';ui.render();return true}
  if(a==='flash-clear'){flashQuery='';clearFilters();ui.render();return true}
  if(a==='affiliate'){ui.go('affiliate');return true}
  if(a==='external'){external(b.dataset.key);return true}
  if(a==='experience-retry'){if(document.readyState)load();else{mode='ready';ui.render()}return true}
  return false;
 }
 function submit(f){const d=new FormData(f),get=k=>HECStore.text(d.get(k),1000);
  if(f.id==='filter-form'){const min=parsePrice(d.get('min')),max=parsePrice(d.get('max'));if(Number.isNaN(min)||Number.isNaN(max)||(min!==null&&max!==null&&min>max)){feedback(T('أدخل نطاق أسعار صحيحاً؛ الحد الأدنى لا يتجاوز الأعلى.','Enter a valid range; minimum cannot exceed maximum.'),'#filter-feedback');return true}priceMin=min;priceMax=max;brands=[];sections=[];for(const [k,v] of d.entries()){if(/^brand\d+$/.test(k)&&HEC.products.some(p=>p.brand===v))brands.push(v);if(/^section\d+$/.test(k)&&HEC.categories.includes(v))sections.push(v)}ui.setFilters({category:get('category')||'الكل',onlyOffers:d.has('offers')});ui.render();return true}
  if(f.id==='search-form'||f.id==='barcode-form'){clearFilters();return false}
  if(f.id==='flash-search-form'){flashQuery=get('q').slice(0,100);ui.render();return true}
  if(f.id==='language-form'){save(s=>s.locale=get('locale')==='en'?'en':'ar');return true}
  if(f.id==='orders-filter-form'){orderQuery=get('q').slice(0,100);orderFilter=['all','draft','sent'].includes(get('status'))?get('status'):'all';ui.render();return true}
  if(f.id==='message-search-form'){messageQuery=get('q').slice(0,100);ui.render();return true}
  const protectedForms=['customer-profile-form','customer-email-form','customer-password-form','wallet-form','return-form','affiliate-form','customer-message-form'];if(!protectedForms.includes(f.id))return false;if(!entered){f.reset();ui.go('account');return true}
  if(f.id==='customer-profile-form'){const name=get('name').slice(0,80),phone=HECStore.digits(get('phone'));if(name.length<2||!HECStore.phone(phone)){ui.toast(T('تحقق من الاسم ورقم الهاتف','Check the name and phone number'));return true}save(s=>s.profile={...s.profile,name,phone})}
  if(f.id==='customer-email-form'){const email=get('email');if(!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email)||email.length>100){ui.toast(T('أدخل إيميلاً صحيحاً','Enter a valid email'));return true}save(s=>s.profile.email=email)}
  if(f.id==='customer-password-form'){const old=get('old'),next=get('new'),confirm=get('confirm');f.reset();if(next.length<12||next.length>128||!old)feedback(T('أكمل الحقول واستخدم 12 محرفاً على الأقل.','Complete all fields and use at least 12 characters.'));else if(next!==confirm)feedback(T('كلمتا المرور غير متطابقتين.','Passwords do not match.'));else if(old===next)feedback(T('اختر كلمة مرور مختلفة عن الحالية.','Choose a different password.'));else feedback(T('الحقول صحيحة. لم تُغيّر أو تُرسل أو تُحفظ كلمة مرور؛ الخدمة غير مفعّلة.','Valid fields. No password was changed, sent or stored; the service is not enabled.'))}
  if(f.id==='wallet-form'){const amount=parsePrice(get('amount'));if(!Number.isSafeInteger(amount)||amount<=0||amount>100000000){ui.toast(T('أدخل مبلغاً صحيحاً من 1 إلى 100000000','Enter a whole amount from 1 to 100000000'));return true}if(state().walletRequests.length>=100){ui.toast(T('وصلت إلى الحد الأقصى للمسودات','Draft limit reached'));return true}save(s=>s.walletRequests.unshift({id:id('W',s.walletRequests),date:date(),type:get('type')==='withdraw'?'withdraw':'deposit',amount,note:get('note').slice(0,300)}))}
  if(f.id==='return-form'){const [oid,index]=get('line').split('/'),o=state().orders.find(o=>o.id===oid),l=o&&o.lines[Number(index)],qty=parsePrice(get('qty'));if(!l||!Number.isInteger(qty)||qty<1||qty>l.qty){ui.toast(T('كمية المرتجع يجب ألا تتجاوز كمية المنتج في الطلب','Return quantity cannot exceed the ordered quantity'));return true}const used=state().returns.filter(r=>r.orderId===oid&&r.lineKey===(l.lineKey||l.id)).reduce((n,r)=>n+r.qty,0);if(used+qty>l.qty){ui.toast(T('توجد مسودة مرتجع لهذه الكمية بالفعل','A return draft already covers this quantity'));return true}if(state().returns.length>=100){ui.toast(T('وصلت إلى الحد الأقصى للمسودات','Draft limit reached'));return true}save(s=>s.returns.unshift({id:id('R',s.returns),date:date(),orderId:oid,productId:l.id,lineKey:l.lineKey||l.id,qty,reason:get('reason'),note:get('note').slice(0,500)}))}
  if(f.id==='affiliate-form'&&(!HECEngagement.effective('affiliate').enabled||!HECEngagement.visible('affiliate'))){ui.toast('التسويق بالعمولة غير مفعّل لحسابك');return true}if(f.id==='affiliate-form'){const name=get('name'),channel=get('channel');if(name.length<2||channel.length<2){ui.toast(T('أكمل الاسم وقناة التسويق','Complete your name and marketing channel'));return true}save(s=>s.affiliateDraft={name,channel,note:get('note'),status:'draft'})}
  if(f.id==='customer-message-form'){const subject=get('subject'),body=get('body');if(subject.length<2||body.length<5){ui.toast(T('أدخل الموضوع ورسالة من خمسة محارف على الأقل','Enter a subject and a message of at least five characters'));return true}const old=state().messages.find(r=>r.id===get('draftId'));if(!old&&state().messages.length>=100){ui.toast(T('وصلت إلى الحد الأقصى للمسودات','Draft limit reached'));return true}save(s=>{const item={id:old?old.id:id('M',s.messages),date:date(),subject:subject.slice(0,100),body,productId:get('productId')};s.messages=old?s.messages.map(r=>r.id===old.id?item:r):[item,...s.messages]})}
  return true;
 }
 function setSlide(n){slide=(n+HEC.homeCampaigns.length)%HEC.homeCampaigns.length;const c=HEC.homeCampaigns[slide],img=document.querySelector('#campaign-image'),link=document.querySelector('#campaign-link');if(img){img.src='assets/'+c[0];img.alt=c[1]}if(link)link.dataset.go=c[2];document.querySelectorAll('[data-action="campaign-slide"]').forEach(b=>{const selected=Number(b.dataset.index)===slide;b.classList.toggle('selected',selected);b.setAttribute('aria-pressed',String(selected))});schedule()}
 function schedule(){clearTimeout(timer);clearTimeout(tick);if(document.hidden)return;if(route()==='home'&&!window.HECMarketing&&!paused&&!(window.matchMedia&&window.matchMedia('(prefers-reduced-motion: reduce)').matches))timer=setTimeout(()=>setSlide(slide+1),Math.max(2,Math.min(60,Number((HEC.homeCampaigns[slide]||[])[3])||5))*1000);updateCountdowns()}
 function updateCountdowns(){clearTimeout(tick);if(document.hidden||!['home','offers','flash'].includes(route()))return;document.querySelectorAll('[data-countdown]').forEach(el=>{const c=HEC.campaigns.find(c=>c.id===el.dataset.countdown);if(c)el.innerHTML=countdown(c)});if(route()==='home'&&!window.HECMarketing&&!paused&&!(window.matchMedia&&window.matchMedia('(prefers-reduced-motion: reduce)').matches))document.querySelectorAll('[data-promo]').forEach(el=>{const group=HEC.promoGroups[el.dataset.promo],img=el.querySelector('img');if(group&&img){const src='assets/'+group[Math.floor((Date.now()-begun)/6500)%group.length];if(img.getAttribute('src')!==src)img.src=src}});tick=setTimeout(updateCountdowns,1000)}
 function skeleton(){return `<div class="skeleton-page" role="status" aria-label="${T('جارٍ التحميل','Loading')}"><div class="skeleton hero-skeleton"></div><div class="skeleton-grid">${Array.from({length:6},()=>'<div class="skeleton tile-skeleton"></div>').join('')}</div>`}
 function asyncState(){if(mode==='loading')return skeleton();if(mode==='error')return empty(T('تعذر تحميل المحتوى','Content could not be loaded'),T('يمكنك إعادة المحاولة. بياناتك المحفوظة لم تتغير.','Try again. Your saved data has not changed.'))+btn(T('إعادة المحاولة','Try again'),'experience-retry');if(mode==='empty')return empty(T('لا توجد بيانات','No data available'),T('لا توجد بيانات متاحة لهذا القسم حالياً.','There is no content in this section right now.'))+btn(T('إعادة المحاولة','Try again'),'experience-retry');return null}
 function render(r){const screens={home,offers,flash,innovative,account,profile,email,password,wallet,returns,loyalty,affiliate,messages,language,notifications,orders};if(['home','shop','offers','wallet','orders'].includes(r)&&mode!=='ready')return page(T('متجر الهيثم','HEC store'),asyncState(),'',true);return screens[r]?screens[r]():null}
 async function load(){const generation=++loadVersion;mode='loading';ui.render();try{const ps=await HEC.repository.listProducts();if(generation!==loadVersion)return;if(!Array.isArray(ps))throw Error('Invalid catalog response');mode=ps.length?'ready':'empty'}catch(_){if(generation!==loadVersion)return;mode='error'}ui.render()}
 function init(context){ui=context;if(!state().campaignEpoch)commit(s=>s.campaignEpoch=Date.now());document.addEventListener('visibilitychange',schedule);document.addEventListener('error',e=>{const img=e.target;if(img&&img.tagName==='IMG'&&!img.dataset.fallback){img.dataset.fallback='true';img.src='assets/hec-product.jpg';img.alt=T('صورة المنتج غير متاحة','Product image unavailable');img.classList.add('image-fallback')}},true);let x=0;document.addEventListener('touchstart',e=>{if(e.target.closest('[data-experience-swipe]'))x=e.changedTouches[0].clientX},{passive:true});document.addEventListener('touchend',e=>{if(e.target.closest('[data-experience-swipe]')&&Math.abs(e.changedTouches[0].clientX-x)>45)setSlide(slide+(e.changedTouches[0].clientX>x?1:-1))},{passive:true})}
 function afterRender(){if(document.documentElement){document.documentElement.lang=state().locale;document.documentElement.dir=state().locale==='en'?'ltr':'rtl'}schedule()}
 function localizeHTML(html){if(state().locale!=='en')return html;return html.replace(/>([^<>]+)</g,(all,v)=>{const t=v.trim();return dictionary[t]?all.replace(t,E(dictionary[t])):all}).replace(/(aria-label|placeholder)="([^"]+)"/g,(all,k,v)=>dictionary[v]?`${k}="${E(dictionary[v])}"`:all)}
 return {init,start(){if(document.readyState)load()},render,click,submit,afterRender,localizeHTML,matches,clearFilters,unread:()=>window.HECNotifications?HECNotifications.unread():HEC.notifications.filter(n=>!state().readNotifications.includes(n.id)).length,countdown,validExternal,setMode(value){mode=['ready','loading','empty','error'].includes(value)?value:'ready'},endPreview(){entered=false;pendingReturn='home'},T};
})();
